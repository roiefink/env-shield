{
  "name": "env-shield",
  "version": "1.0.0",
  "description": "A user-friendly utility for validating and parsing environment variables effortlessly.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "prepublish": "npm run build",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/roiefink/env-shield.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/roiefink/env-shield/issues"
  },
  "homepage": "https://github.com/roiefink/env-shield#readme",
  "devDependencies": {
    "@types/node": "^20.8.2",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "prettier": "3.0.3",
    "typescript": "^5.2.2"
  }
}
